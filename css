<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Styled Page</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>

    <h1 class="title">Welcome to My Styled Page</h1>
    <p class="content">This is an example of applying CSS to style elements effectively.</p>

    <img id="styled-image" src="image.jpg" alt="Beautiful Scenery">

    <div class="container">
        <p>CSS makes web pages look great!</p>
    </div>

</body>
</html>
/* Using a class selector */
.title {
    font-family: 'Arial', sans-serif;
    color: navy;
    text-align: center;
}

/* Using an ID selector */
#styled-image {
    width: 300px;
    border: 5px solid #333;
    margin: 20px;
    padding: 10px;
}

/* Using an element selector */
body {
    font-family: 'Verdana', sans-serif;
    margin: 40px;
    padding: 20px;
    background-color: #f5f5f5;
}

/* Styling a container */
.container {
    background-color: lightgray;
    padding: 15px;
    border-radius: 8px;
    text-align: center;
}
